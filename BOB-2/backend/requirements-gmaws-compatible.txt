# Compatibility profile for using the safe GMAWS workflow concepts inside BOB-2.
#
# This file intentionally avoids the robot simulation stack from GMAWS:
# - pybullet
# - trimesh
# - numpy<2 pin
#
# Reason: BOB-2 is an accounting/audit FastAPI + Next.js application, while the
# uploaded GMAWS project contains robot-design and simulation tooling. Pulling
# the robotics dependencies into the accounting backend would increase install
# failures on Windows/WSL and can create unnecessary version conflicts.

-r requirements.txt

# Lightweight dependencies used by the reusable non-robot GMAWS concepts.
requests>=2.34.2,<3.0.0
pyyaml>=6.0.0,<7.0.0
aiohttp>=3.14.3,<4.0.0
beautifulsoup4>=4.15.0,<5.0.0
